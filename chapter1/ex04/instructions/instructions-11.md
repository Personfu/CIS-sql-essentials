<details>
<summary> <b>Want a hint?</b> </summary>
<ul>
    <li>For this new column, use the same code that you used for the last column, but multiply it by the `original_price` field.</li>
	<li>Remember, you **cannot** reference a column name (like `discount_amount`) within the same query, so you will have to copy the calculation you used previously. Be careful of parentheses!</li>
<ul>
</details>
