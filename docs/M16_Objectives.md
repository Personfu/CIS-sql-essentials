# Module 16 Objectives

## What you will learn
- Create triggers for validation and auditing
- Build an audit table for changed product records
- Use `BEFORE INSERT` and `AFTER UPDATE` triggers
- Schedule cleanup with MySQL events
