# Subqueries and Derived Tables

Use subqueries to compare rows and calculate averages for a subset of data.

## Assignment

Create an Employees table and build a view showing employees with above-average salary by department.

## Files

- `assignment.sql` — student SQL script
- `tests.sql` — hidden grading queries
- `expected_output.txt` — expected test output

Do not edit `tests.sql` or `expected_output.txt`.
